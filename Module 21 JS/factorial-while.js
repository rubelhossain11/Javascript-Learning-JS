/* function factorial(number) {
  let result = 1;
  let i = number;
  while (i >= 1) {
    result = result * i;
    i--;
  }
  return result;
}
const myNumber = factorial(7);
console.log("factorial is 7:", myNumber);
const herNumber = factorial(8);
console.log("factorial is 8:", herNumber);
const korimNumber = factorial(10);
console.log("factorial is 10:", korimNumber);
 */
// using function 11 factorial by while loop using
/* function factorial(number) {
  let result = 1;

  for (let i = number; i >= 1; i--) {
    result = result * i;
  }
  return result;
}
var myNumber = factorial(11);
console.log("the number of 11 factorial is:", myNumber); */

/* function factorial(number) {
  let result = 1;
  let i = number;
  while (i >= 1) {
    result = result * i;
    i--;
  }
  return result;
}
var myNumber = factorial(11);
console.log("the number of 11 factorial is:", myNumber);
 */
/* function factorial(number) {
  let result = 1;
  for (let i = number; i >= 1; i--) {
    if (number == 0) {
      return 1;
    } else {
    }
    return result * factorial(result - 1);
  }
} */
let myNumber = factorial(10);
console.log("the number factorial 10 is:", myNumber);

/* function factorialize(num) {
  if (num < 0) 
        return -1;
  else if (num == 0) 
      return 1;
  else {
      return (num * factorialize(num - 1));
  }
}
factorialize(5); */
