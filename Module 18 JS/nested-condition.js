var isGraduated = true;
var mySalary = 3000;
var car = 0;
if (isGraduated === false || 4000 < mySalary) {
  console.log("finally we matured");
  {
    if (isGraduated === false || 5000 < mySalary) {
      console.log("lets married we");
    } else if (isGraduated === true || 5000 < mySalary) {
      console.log("next year we married");
    } else {
      console.log("amader kopal a biye nai");
    }
  }
} else {
  console.log("we are separet now");
}
